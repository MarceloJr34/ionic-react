import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.MarceloJr.BmiCalculator',
  appName: 'BmiCalculator',
  webDir: 'dist'
};

export default config;
